div(form(different thing to vote for onclick hide the set up buttons)buttons()div(space for result)) -- html
firebase setup
send input to the database
get input and write it to the div for results -- js